# Capital Allocation CI contract

`validate_capital_allocation.py` must pass before Pages deploy. It enforces fail-closed opportunity alternatives, no portfolio upgrade from VERIFY/WATCH to BUY, scenario probability consistency, research-queue provenance, and no personalized sizing without current portfolio state.
